image: node:latest

stages:
  - build
  - test

cache:
  paths:
    - node_modules

install_dependencies:
  stage: build
  script:
    - yarn install
  artifacts:
    paths:
      - node_modules/

code_test:
  stage: test
  script: yarn lint